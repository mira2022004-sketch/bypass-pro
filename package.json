{
  "name": "bypass-pro",
  "version": "1.0.0",
  "private": true,
  "description": "Bypass Pro - Reconnect Tool com ativação por chave",
  "scripts": {
    "start:server": "cd server && npm start",
    "build": "cd server && npm install"
  }
}
